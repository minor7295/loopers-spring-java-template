server:
  shutdown: graceful
  tomcat:
    threads:
      max: 200 # 최대 워커 스레드 수 (default : 200)
      min-spare: 10 # 최소 유지 스레드 수 (default : 10)
    connection-timeout: 1m # 연결 타임아웃 (ms) (default : 60000ms = 1m)
    max-connections: 8192 # 최대 동시 연결 수 (default : 8192)
    accept-count: 100 # 대기 큐 크기 (default : 100)
    keep-alive-timeout: 60s # 60s
  max-http-request-header-size: 8KB

spring:
  main:
    web-application-type: servlet
  application:
    name: commerce-api
  profiles:
    active: local
  config:
    import:
      - jpa.yml
      - redis.yml
      - logging.yml
      - monitoring.yml
  batch:
    jdbc:
      initialize-schema: always # Spring Batch 메타데이터 테이블 자동 생성 (임시: production 배포 전 EDA로 교체 예정)
    job:
      enabled: false # 스케줄러에서 수동 실행하므로 자동 실행 비활성화

payment-gateway:
  url: http://localhost:8082

feign:
  client:
    config:
      default:
        connectTimeout: 2000 # 연결 타임아웃 (2초)
        readTimeout: 6000 # 읽기 타임아웃 (6초) - PG 처리 지연 1s~5s 고려
      paymentGatewayClient:
        connectTimeout: 2000 # 연결 타임아웃 (2초)
        readTimeout: 6000 # 읽기 타임아웃 (6초) - PG 처리 지연 1s~5s 고려
        loggerLevel: full # 로깅 레벨 (디버깅용)
  circuitbreaker:
    enabled: true # CircuitBreaker 활성화
  resilience4j:
    enabled: true # Resilience4j 활성화

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10 # 슬라이딩 윈도우 크기
        minimumNumberOfCalls: 5 # 최소 호출 횟수 (이 횟수 이상 호출되어야 통계 수집)
        permittedNumberOfCallsInHalfOpenState: 3 # Half-Open 상태에서 허용되는 호출 수
        automaticTransitionFromOpenToHalfOpenEnabled: true # 자동으로 Half-Open으로 전환
        waitDurationInOpenState: 10s # Open 상태 유지 시간 (10초 후 Half-Open으로 전환)
        failureRateThreshold: 50 # 실패율 임계값 (50% 이상 실패 시 Open)
        slowCallRateThreshold: 100 # 느린 호출 비율 임계값 (100% - 모든 호출이 느림)
        slowCallDurationThreshold: 3s # 느린 호출 기준 시간 (3초 이상)
        recordExceptions:
          - feign.FeignException
          - java.net.SocketTimeoutException
          - java.util.concurrent.TimeoutException
    instances:
      paymentGatewayClient:
        baseConfig: default
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        waitDurationInOpenState: 10s
        failureRateThreshold: 50
        # 실패율 임계값 50%: 최소 5번 호출 중 3번 이상 실패 시 Open 상태로 전환
        # 이 설정은 Retry와 함께 사용될 때 적절한 균형을 제공합니다:
        # - 너무 낮으면 (예: 30%): 정상적인 일시적 오류에도 Fallback이 과도하게 호출됨
        # - 너무 높으면 (예: 70%): 실제 장애 상황에서 Fallback이 늦게 활성화됨
        # 50%는 일반적인 마이크로서비스 환경에서 권장되는 값입니다
  retry:
    configs:
      default:
        maxAttempts: 3 # 최대 재시도 횟수 (초기 시도 포함)
        waitDuration: 500ms # 재시도 대기 시간 (기본값, paymentGatewayClient는 Java Config에서 Exponential Backoff 적용)
        retryExceptions:
          # 일시적 오류만 재시도: 5xx 서버 오류, 타임아웃, 네트워크 오류
          - feign.FeignException$InternalServerError # 500 에러
          - feign.FeignException$ServiceUnavailable   # 503 에러
          - feign.FeignException$GatewayTimeout      # 504 에러
          - java.net.SocketTimeoutException
          - java.util.concurrent.TimeoutException
        ignoreExceptions:
          # 클라이언트 오류(4xx)는 재시도하지 않음: 비즈니스 로직 오류이므로 재시도해도 성공하지 않음
          - feign.FeignException$BadRequest      # 400 에러
          - feign.FeignException$Unauthorized    # 401 에러
          - feign.FeignException$Forbidden       # 403 에러
          - feign.FeignException$NotFound         # 404 에러
    instances:
      paymentGatewayClient:
        baseConfig: default
        maxAttempts: 3
        # waitDuration은 Java Config(Resilience4jRetryConfig)에서 Exponential Backoff로 덮어씀
        # Exponential Backoff: 초기 500ms → 1000ms → 최대 5초 (랜덤 jitter 포함)
        waitDuration: 500ms # 참고용 (실제로는 Java Config의 Exponential Backoff가 적용됨)
  timelimiter:
    configs:
      default:
        timeoutDuration: 6s # 타임아웃 시간 (Feign readTimeout과 동일)
        cancelRunningFuture: true # 실행 중인 Future 취소
    instances:
      paymentGatewayClient:
        baseConfig: default
        timeoutDuration: 6s

springdoc:
  use-fqn: true
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: local, test

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: qa

---
spring:
  config:
    activate:
      on-profile: prd

springdoc:
  api-docs:
    enabled: false